{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npx prisma generate"
  },
  "deploy": {
    "startCommand": "npx tsx prisma/seed-demo.ts",
    "cronSchedule": "0 7 * * *",
    "restartPolicyType": "NEVER"
  }
}
