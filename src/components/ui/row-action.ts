/**
 * Shared className for secondary row-action buttons (≥44px touch target).
 * Import this constant in all panels instead of duplicating the string.
 */
export const rowActionBtn =
  "h-11 w-11 flex items-center justify-center rounded-lg transition-all " +
  "active:scale-90 motion-reduce:active:scale-100 shrink-0 " +
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brick " +
  "focus-visible:ring-offset-2 focus-visible:ring-offset-surface";
